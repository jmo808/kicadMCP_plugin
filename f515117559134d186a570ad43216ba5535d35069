Task: Implement component abstraction registry (kbd_engine/registry.py)

Description: Implemented the FootprintRegistry class that maps logical components to physical KiCad footprint references. Configured registry parsing from a JSON file, establishing system-wide defaults (MX switch, SOD-123 diode, 0805 capacitor) in default_registry.json, and supporting per-key overrides (e.g. SW_0_5 using a custom spacebar switch). Wrote unit tests confirming default resolution, error handling for unknown components, and JSON-based overrides.

Modified Files:
- kbd_engine/registry.py
- kbd_engine/data/default_registry.json
- tests/test_registry.py
